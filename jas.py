# Name = input("Enter the student Name:")
# Age = int(input("Enter the student Age:"))
# Class = input("Enter the student CLass:")
# print("Student Name:", Name)
# print("Student Age:", Age)
# print("Student Class:", Class)

# This code snippet is taking user input for the variable `a` as an integer, then it is attempting to convert the variable `a` to its type using the `type()` function. However, the `type()` function in this context will return the type of the variable `a`, not convert it to a different type.
a = int(input("Enter the first number: "))

a = float(a)
print("The value of a is:", a)
